#############################################################
# ? Test Automation Configuration File
#############################################################

# Browser Options ? chrome | firefox | edge
browser = chrome

# Mode ? headed | headless
mode = headed

# Application URL
url = https://www.makemytrip.com/

# Folder to store screenshots (auto created if not exists)
screenshotPath = screenshots


#############################################################
# (Optional / Future Ready Fields)
#############################################################

# Timeout in seconds (UI element waits)
implicitWait = 10
explicitWait = 15

# Enable logs ? true / false
logging = true

# Retry failed test execution attempts
retryCount = 0
